# Free Proxy add-ons

Home Assistant add-on repository for [Free Proxy](free_proxy/): an OpenAI-compatible proxy for zero-price OpenCode and OpenRouter models.

Build the add-on image locally:

```sh
docker build -t free-proxy ./free_proxy
```

See [`free_proxy/README.md`](free_proxy/README.md) for configuration and API details.
